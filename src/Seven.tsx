const Seven = () => {
  return (
    <section aria-labelledby="seven">
      <h2 id="seven">7. Dynamically add child components</h2>
      <p>(already done in #1)</p>
    </section>
  );
};

export default Seven;
